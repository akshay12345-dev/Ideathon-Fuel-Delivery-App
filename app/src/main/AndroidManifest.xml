<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.idea.fuel1">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.    READ_GSERVICES" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".GoogleMap2"
            android:label="@string/title_activity_google_map2"></activity>
        <activity
            android:name=".GoogleMap1"
            android:label="@string/title_activity_google_map1" />
        <activity android:name=".deliver" />
        <activity android:name=".BookDetails" />
        <activity android:name=".Payment" />
        <activity android:name=".CustomerBook" />
        <activity
            android:name=".AdminTask2"
            android:label="@string/title_activity_admin_task2"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Service2" />
        <activity android:name=".AdminSignNext2" />
        <activity android:name=".AdminSignUp2" />
        <activity android:name=".AdminLogin2" />
        <activity android:name=".services" />
        <activity
            android:name=".AdminMap"
            android:label="@string/title_activity_admin_map" />
        <activity android:name=".AdminSignNext" />
        <activity android:name=".AdminSignUp" />
        <activity android:name=".AdminLogin" />
        <activity android:name=".Option" />
        <activity
            android:name=".Bottom"
            android:label="@string/title_activity_bottom" />
        <activity android:name=".SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignUpNext" />
        <activity android:name=".SignUp" />
        <activity android:name=".LogIn" />
    </application>

</manifest>